# Инструкция по сборке проекта

## Основные ошибки были исправлены:

### 1. ✅ build.gradle.kts
- Исправлен неправильный синтаксис `compileSdk { version = release(36) }` → `compileSdk = 35`
- Удалена лишняя зависимость `protolite.well.known.types`
- Изменен targetSdk с 36 на 35 (стабильная версия)

### 2. ✅ MainScreen.kt
- Заменены несуществующие иконки на стандартные
- Исправлены полноэкранные диалоги
- Добавлены явные импорты

### 3. ✅ BluetoothManager.kt
- Добавлена поддержка всех BLE устройств
- Автоматическое обнаружение сервисов и характеристик
- Улучшена стабильность подключения к ESP32

## Как собрать проект:

### Шаг 1: Синхронизация
В Android Studio:
1. Нажмите `File` → `Sync Project with Gradle Files`
2. Или нажмите кнопку с иконкой слона 🐘 в верхней панели
3. Дождитесь завершения синхронизации (может занять 1-2 минуты)

### Шаг 2: Очистка
1. `Build` → `Clean Project`
2. Дождитесь завершения

### Шаг 3: Сборка
1. `Build` → `Rebuild Project`
2. Дождитесь завершения (первая сборка может занять 3-5 минут)

### Шаг 4: Запуск
1. Подключите Android устройство или запустите эмулятор
2. Нажмите кнопку Run (▶️) или `Shift + F10`
3. Выберите устройство

## Если все равно ошибки:

### Проверьте версию Android Studio
Требуется: **Android Studio Hedgehog (2023.1.1) или новее**

Проверить: `Help` → `About Android Studio`

### Проверьте JDK
Требуется: **JDK 11 или 17**

Проверить: `File` → `Project Structure` → `SDK Location` → `JDK Location`

### Проверьте версию Gradle
Файл `gradle/wrapper/gradle-wrapper.properties` должен содержать:
```properties
distributionUrl=https\://services.gradle.org/distributions/gradle-8.4-bin.zip
```

### Очистите кэш
1. `File` → `Invalidate Caches...`
2. Выберите все опции
3. Нажмите `Invalidate and Restart`

### Проверьте libs.versions.toml
Убедитесь, что в файле `gradle/libs.versions.toml` есть все зависимости:
```toml
[versions]
agp = "8.13.1"
kotlin = "2.0.21"
navigationCompose = "2.8.4"
...
```

## Типичные ошибки и решения:

### Ошибка: "Cannot resolve symbol 'Icons'"
**Решение:** Синхронизируйте проект с Gradle

### Ошибка: "Unresolved reference: SearchView"
**Решение:** 
1. Проверьте, что файлы существуют в `app/src/main/java/com/example/bluetooth111/ui/screens/`
2. Синхронизируйте проект

### Ошибка: "Gradle sync failed"
**Решение:**
1. Проверьте интернет-соединение
2. Очистите кэш Gradle: удалите папку `~/.gradle/caches/`
3. Перезапустите Android Studio

### Ошибка: "SDK 35 not found"
**Решение:**
1. `Tools` → `SDK Manager`
2. Установите `Android 14.0 (API 35)`
3. Нажмите `Apply` и дождитесь установки

## Проверка после сборки:

После успешной сборки в `app/build/outputs/apk/debug/` появится файл:
- `app-debug.apk` - можно установить на устройство

## Тестирование с ESP32:

1. Загрузите код из `ESP32_SETUP.md` на ESP32
2. Запустите приложение
3. Перейдите на вкладку "Поиск"
4. Нажмите "Начать поиск"
5. Найдите ваш ESP32 и подключитесь
6. Статус должен измениться на "Connected" или "Ready"

## Логирование для отладки:

В Android Studio откройте **Logcat** и фильтруйте по тегам:
- `BluetoothManager` - для отладки Bluetooth
- `MainActivity` - для отладки основного приложения

## Контакты для поддержки:

Если проблемы остаются:
1. Проверьте версии всех инструментов
2. Очистите все кэши
3. Перезагрузите компьютер
4. Попробуйте на другом устройстве/эмуляторе

Проект готов к использованию! 🚀


